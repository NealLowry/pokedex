#ifndef TYPES_HPP_
#define TYPES_HPP_

#include <unordered_map>
#include <string>
#include <vector>

struct Types{
    std::vector<std::string> normal();
    std::vector<std::string> grass();
    std::vector<std::string> fire();
    std::vector<std::string> water();
    std::vector<std::string> electric();
    std::vector<std::string> rock();
    std::vector<std::string> poison();
    std::vector<std::string> psychic();
    std::vector<std::string> ice();
    std::vector<std::string> ground();
    std::vector<std::string> flying();
    std::vector<std::string> fighting();
    std::vector<std::string> bug();
    std::vector<std::string> ghost();
    std::vector<std::string> dragon();
    std::vector<std::string> dark();
    std::vector<std::string> steel();
    std::vector<std::string> fairy();
};

#endif /* TYPES_HPP_ */